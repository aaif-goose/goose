[pytest]
# Skein-only Python test config. Upstream goose has no top-level pytest config,
# so this file is purely additive and does not collide on upstream merges.
testpaths =
    eval-bench/tests
    recipes
addopts = -ra
